import React from 'react'
import {
  Linking,
  SafeAreaView,
  StyleSheet,
  Text,
  TouchableOpacity,
} from 'react-native'

export default function DrawerContent () {
  return (
    <SafeAreaView style={{flex: 1, backgroundColor: 'green'}}></SafeAreaView>
  )
}
